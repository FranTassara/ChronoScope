"""Third-party code vendored into ChronoScope.

These packages are bundled rather than installed from PyPI because they are not
distributed as installable packages, or because ChronoScope pins a specific
revision. Each subpackage keeps the upstream source URL in its own __init__.py.
"""
